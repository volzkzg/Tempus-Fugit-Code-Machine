#include <cstdio>
#include <cstring>
#include <iostream>
#include <algorithm>
#include <cstdlib>

using namespace std;

int main()
{
	int T = 10;
	printf("%d\n", T);
	int n, h, w;
	n = 50000, h, w;
	h = w = (1 << 15) - 1;
	
	return 0;
}
